<%= form_for(@user_uploaded_image) do |f| %>
  <% if @user_uploaded_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_uploaded_image.errors.count, "error") %> prohibited this user_uploaded_image from being saved:</h2>

      <ul>
      <% @user_uploaded_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
